/**
 * Handles Trailer changes.
 * @model com/renault/parkassist/viewmodel/trailer
 */
interface TrailerViewModel {

    /** Weather the trailer is connected to the car or not.
     * @default false
     */
    presence: boolean;
}